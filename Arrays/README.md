•Arrays in javascript are NON-homogenous(elements in array can have different datatypes) ps: u can even add emojis in array as string
                                          u can even have objects in array
